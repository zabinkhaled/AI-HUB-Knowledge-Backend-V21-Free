import {fetchWithTimeout} from '../core/http.mjs';
export async function searchYouTube({query,language='en',maxResults=6,timeoutMs=8000}){const key=process.env.YOUTUBE_API_KEY;if(!key)return{connector:'youtube',status:'disabled',results:[],reason:'YOUTUBE_API_KEY is not configured'};const url=new URL('https://www.googleapis.com/youtube/v3/search');url.searchParams.set('part','snippet');url.searchParams.set('q',query);url.searchParams.set('type','video');url.searchParams.set('maxResults',String(Math.min(maxResults,50)));if(language&&language!=='auto')url.searchParams.set('relevanceLanguage',language);url.searchParams.set('safeSearch','moderate');url.searchParams.set('key',key);const r=await fetchWithTimeout(url,{headers:{Accept:'application/json'}},timeoutMs);if(!r.ok)throw new Error(`YouTube API ${r.status}`);const data=await r.json();return{connector:'youtube',status:'ok',results:(data.items||[]).map(x=>({id:x.id.videoId,type:'video',title:x.snippet?.title||'',description:x.snippet?.description||'',url:`https://www.youtube.com/watch?v=${x.id.videoId}`,embedUrl:`https://www.youtube.com/embed/${x.id.videoId}`,thumbnail:x.snippet?.thumbnails?.high?.url||x.snippet?.thumbnails?.default?.url,publishedAt:x.snippet?.publishedAt,author:x.snippet?.channelTitle,source:'YouTube',sourceType:'video'}))}}
