import {fetchWithTimeout} from '../core/http.mjs';
export async function searchGitHub({query,maxResults=6,timeoutMs=8000}){const url=new URL('https://api.github.com/search/repositories');url.searchParams.set('q',query);url.searchParams.set('per_page',String(Math.min(maxResults,30)));const headers={'Accept':'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28'};if(process.env.GITHUB_TOKEN)headers.Authorization=`Bearer ${process.env.GITHUB_TOKEN}`;const r=await fetchWithTimeout(url,{headers},timeoutMs);if(!r.ok)throw new Error(`GitHub API ${r.status}`);const data=await r.json();return{connector:'github',status:'ok',results:(data.items||[]).map(x=>({id:String(x.id),type:'repository',title:x.full_name,description:x.description||'',url:x.html_url,publishedAt:x.created_at,updatedAt:x.updated_at,author:x.owner?.login,source:'GitHub',sourceType:'repository'}))}}
